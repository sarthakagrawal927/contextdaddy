name: Protected manual release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Exact v<major>.<minor>.<patch>-<build> tag on the current main commit
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: manual-production-release
  cancel-in-progress: false

jobs:
  release:
    if: github.ref == 'refs/heads/main'
    environment: production-release
    runs-on: macos-26
    timeout-minutes: 75
    steps:
      - name: Check out release branch
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: main
          path: app
          fetch-depth: 0
          persist-credentials: false
      - name: Check out pinned public validation tooling
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          repository: sass-maker/saas-maker
          ref: 7a3b78acbcd523bb968ff19e1b3ae9427d022b91
          path: .daddy-tooling
          persist-credentials: false
      - name: Verify release tag and source commit
        id: source
        env:
          RELEASE_TAG: ${{ inputs.tag }}
          CALLER_REPOSITORY: ${{ github.repository }}
        run: |
          python3 .daddy-tooling/tooling/daddy-macos/release_preflight.py \
            --app contextdaddy \
            --repository "$CALLER_REPOSITORY" \
            --root app \
            --tag "$RELEASE_TAG" \
            --receipt release-source-receipt.json
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path
          source_sha = json.loads(Path('release-source-receipt.json').read_text())['sourceSha']
          with open(os.environ['GITHUB_OUTPUT'], 'a') as output:
              output.write(f'source_sha={source_sha}\n')
          PY
      - name: Check out exact release source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ steps.source.outputs.source_sha }}
          path: app
          persist-credentials: false
      - name: Run candidate checks on release source
        env:
          CALLER_REPOSITORY: ${{ github.repository }}
        run: |
          python3 .daddy-tooling/tooling/daddy-macos/candidate.py \
            --app contextdaddy \
            --repository "$CALLER_REPOSITORY" \
            --root app \
            --receipt release-candidate-receipt.json
      - name: Record preflight receipt
        run: |
          echo '### Exact source gate' >> "$GITHUB_STEP_SUMMARY"
          echo 'The exact tagged source passed candidate checks. Packaging and notarization follow in this job.' >> "$GITHUB_STEP_SUMMARY"
          cat release-source-receipt.json release-candidate-receipt.json >> "$GITHUB_STEP_SUMMARY"
      - name: Require protected signing and notarization inputs
        env:
          CERT_READY: ${{ secrets.DEVELOPER_ID_CERT_P12_BASE64 != '' && secrets.DEVELOPER_ID_CERT_PASSWORD != '' && secrets.DEVELOPER_ID_IDENTITY != '' }}
          NOTARY_READY: ${{ secrets.APPLE_NOTARY_API_KEY_P8_BASE64 != '' && secrets.APPLE_NOTARY_KEY_ID != '' && secrets.APPLE_NOTARY_ISSUER_ID != '' }}
          SITE_READY: ${{ secrets.CLOUDFLARE_API_TOKEN != '' && secrets.CLOUDFLARE_ACCOUNT_ID != '' }}
        run: |
          if [[ "$CERT_READY" != true || "$NOTARY_READY" != true || "$SITE_READY" != true ]]; then
            echo 'Protected signing, notary, or Cloudflare inputs are not configured' >&2
            exit 1
          fi
      - name: Resolve exact release version
        id: version
        env:
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          python3 - <<'PY'
          import os
          import re
          match = re.fullmatch(r"v(\d+\.\d+\.\d+)-([1-9]\d*)", os.environ["RELEASE_TAG"])
          if not match:
              raise SystemExit("Invalid release tag")
          with open(os.environ["GITHUB_OUTPUT"], "a") as output:
              output.write(f"version={match.group(1)}\nbuild={match.group(2)}\n")
          PY
      - name: Import protected signing inputs
        env:
          CERT_P12: ${{ secrets.DEVELOPER_ID_CERT_P12_BASE64 }}
          CERT_PASSWORD: ${{ secrets.DEVELOPER_ID_CERT_PASSWORD }}
          NOTARY_P8: ${{ secrets.APPLE_NOTARY_API_KEY_P8_BASE64 }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import base64
          import os
          from pathlib import Path
          temp = Path(os.environ["RUNNER_TEMP"])
          (temp / "signing.p12").write_bytes(base64.b64decode(os.environ["CERT_P12"], validate=True))
          key = temp / "notary.p8"
          key.write_bytes(base64.b64decode(os.environ["NOTARY_P8"], validate=True))
          key.chmod(0o600)
          PY
          keychain="$RUNNER_TEMP/daddy-release.keychain-db"
          password="$(openssl rand -hex 24)"
          security create-keychain -p "$password" "$keychain"
          security set-keychain-settings -lut 21600 "$keychain"
          security unlock-keychain -p "$password" "$keychain"
          security import "$RUNNER_TEMP/signing.p12" -k "$keychain" -P "$CERT_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k "$password" "$keychain"
          security list-keychains -d user -s "$keychain"
      - name: Recover pinned ccusage helper from prior public release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/prior" "$RUNNER_TEMP/prior-mount"
          gh release download v0.1.0-1 -R sarthakagrawal927/contextdaddy \
            --pattern 'ContextDaddy-0.1.0-arm64.dmg' --dir "$RUNNER_TEMP/prior"
          python3 - <<'PY'
          import hashlib
          import os
          from pathlib import Path
          path = Path(os.environ["RUNNER_TEMP"]) / "prior/ContextDaddy-0.1.0-arm64.dmg"
          if hashlib.sha256(path.read_bytes()).hexdigest() != "6f01b6b90813064b0e87820860531a43c0930174d52769afc691679afe6e8a95":
              raise SystemExit("Prior public DMG checksum mismatch")
          PY
          hdiutil attach -readonly -nobrowse -mountpoint "$RUNNER_TEMP/prior-mount" \
            "$RUNNER_TEMP/prior/ContextDaddy-0.1.0-arm64.dmg"
          cp "$RUNNER_TEMP/prior-mount/ContextDaddy.app/Contents/Helpers/ccusage" "$RUNNER_TEMP/ccusage"
          hdiutil detach "$RUNNER_TEMP/prior-mount"
          python3 - <<'PY'
          import hashlib
          import os
          from pathlib import Path
          path = Path(os.environ["RUNNER_TEMP"]) / "ccusage"
          if hashlib.sha256(path.read_bytes()).hexdigest() != "70611f8dce1e4594316ba58c1f4ad62f42c17e4f3d3e7be72b23b4b9aca9e2aa":
              raise SystemExit("Pinned ccusage helper checksum mismatch")
          PY
          "$RUNNER_TEMP/ccusage" --version
      - name: Sign and notarize ContextDaddy candidate
        env:
          SIGNING_IDENTITY: ${{ secrets.DEVELOPER_ID_IDENTITY }}
          NOTARY_KEY_ID: ${{ secrets.APPLE_NOTARY_KEY_ID }}
          NOTARY_ISSUER_ID: ${{ secrets.APPLE_NOTARY_ISSUER_ID }}
          VERSION: ${{ steps.version.outputs.version }}
          BUILD: ${{ steps.version.outputs.build }}
          SOURCE_SHA: ${{ steps.source.outputs.source_sha }}
        run: |
          python3 app/scripts/release-contextdaddy.py --identity "$SIGNING_IDENTITY" \
            --notary-api-key "$RUNNER_TEMP/notary.p8" --notary-key-id "$NOTARY_KEY_ID" \
            --notary-issuer-id "$NOTARY_ISSUER_ID" --ccusage "$RUNNER_TEMP/ccusage" \
            --output "$RUNNER_TEMP/release" --version "$VERSION" --build "$BUILD" \
            --source-sha "$SOURCE_SHA"
      - name: Validate release metadata
        env:
          RELEASE_TAG: ${{ inputs.tag }}
          SOURCE_SHA: ${{ steps.source.outputs.source_sha }}
          CALLER_REPOSITORY: ${{ github.repository }}
          VERSION: ${{ steps.version.outputs.version }}
          BUILD: ${{ steps.version.outputs.build }}
        run: |
          python3 .daddy-tooling/tooling/daddy-macos/release_contract.py \
            --app contextdaddy --repository "$CALLER_REPOSITORY" --tag "$RELEASE_TAG" \
            --plist "$RUNNER_TEMP/release/image-contents/ContextDaddy.app/Contents/Info.plist" \
            --dmg "$RUNNER_TEMP/release/ContextDaddy-$VERSION-$BUILD-arm64.dmg" \
            --sums "$RUNNER_TEMP/release/SHA256SUMS" \
            --notary-json "$RUNNER_TEMP/release/notarization.json" \
            --previous-build "$(python3 -c 'import json; print(json.load(open("app/site/release.json"))["build"])')" --source-sha "$SOURCE_SHA" \
            --receipt "$RUNNER_TEMP/release/release-metadata-receipt.json"
      - name: Retain qualified release candidate
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: contextdaddy-${{ inputs.tag }}-qualified
          path: |
            ${{ runner.temp }}/release/*.dmg
            ${{ runner.temp }}/release/notarization.json
            ${{ runner.temp }}/release/release-receipt.json
            ${{ runner.temp }}/release/SHA256SUMS
            ${{ runner.temp }}/release/release-metadata-receipt.json
          if-no-files-found: error
          retention-days: 7

      - name: Check out site source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: main
          path: site-source
          fetch-depth: 0
          persist-credentials: false
      - name: Stage qualified site assets
        env:
          CALLER_REPOSITORY: ${{ github.repository }}
        run: |
          python3 - <<'PY'
          import json
          import subprocess
          main_at_check = json.load(open("release-source-receipt.json"))["mainShaAtCheck"]
          site_source = subprocess.check_output(["git", "-C", "site-source", "rev-parse", "HEAD"], text=True).strip()
          if site_source != main_at_check:
              raise SystemExit("main changed after release preflight; start a fresh manual release")
          PY
          dmg="$(find "$RUNNER_TEMP/release" -maxdepth 1 -name '*.dmg' -print -quit)"
          python3 .daddy-tooling/tooling/daddy-macos/publish_site.py --app contextdaddy --repository "$CALLER_REPOSITORY" --site site-source/site --dmg "$dmg" --receipt "$RUNNER_TEMP/release/release-metadata-receipt.json" --output "$RUNNER_TEMP/publication.json"
      - name: Select Node.js for the site
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: site-source/site/package-lock.json
      - name: Install site dependencies
        working-directory: site-source/site
        run: npm ci
      - name: Deploy qualified site
        working-directory: site-source/site
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npm run deploy
      - name: Verify live download and update feed
        run: |
          dmg="$(find "$RUNNER_TEMP/release" -maxdepth 1 -name '*.dmg' -print -quit)"
          python3 .daddy-tooling/tooling/daddy-macos/verify_live.py --report "$RUNNER_TEMP/publication.json" --dmg "$dmg" --output "$RUNNER_TEMP/live-publication-receipt.json"
          cat "$RUNNER_TEMP/live-publication-receipt.json" >> "$GITHUB_STEP_SUMMARY"
      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ inputs.tag }}
          SOURCE_SHA: ${{ steps.source.outputs.source_sha }}
        run: |
          dmg="$(find "$RUNNER_TEMP/release" -maxdepth 1 -name '*.dmg' -print -quit)"
          gh release create "$RELEASE_TAG" "$dmg" "$RUNNER_TEMP/release/SHA256SUMS" "$RUNNER_TEMP/release/release-metadata-receipt.json" "$RUNNER_TEMP/live-publication-receipt.json" --verify-tag --title "$RELEASE_TAG" --notes "Developer ID signed and Apple notarized. Source: $SOURCE_SHA. The live download was verified against the qualified artifact."
      - name: Record published site metadata on main
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          git -C site-source config user.name 'github-actions[bot]'
          git -C site-source config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git -C site-source add -u site
          if ! git -C site-source diff --cached --quiet; then
            git -C site-source commit -m "release: record $RELEASE_TAG site publication"
            gh auth setup-git
            git -C site-source push origin HEAD:main
          fi
